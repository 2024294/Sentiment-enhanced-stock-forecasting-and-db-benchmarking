## 📂 Datasets

- `data/raw/` contains the original, unmodified datasets.
- `data/processed/` contains selected merged datasets used directly for model training.

All processed datasets were generated from the raw data using the notebooks provided in the repository.
Large intermediate files are excluded for reproducibility and storage efficiency.
